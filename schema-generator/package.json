{
  "name": "schema-generator",
  "version": "1.0.0",
  "description": "Schema generator for static data files",
  "main": "src/schema-generator.ts",
  "scripts": {
    "build": "ncc build src/schema-generator.ts -o dist",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "start": "node dist/index.js"
  },
  "keywords": [
    "schema",
    "generator",
    "static-data"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.1",
    "@types/pluralize": "^0.0.33",
    "@vercel/ncc": "^0.38.3",
    "jest": "^30.1.3",
    "ts-jest": "^29.4.4",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "pluralize": "^8.0.0"
  }
}
